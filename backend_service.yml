apiVersion: v1
kind: Service
metadata:
  name: backend_service
  namespace: argocd
spec:
  type: LoadBalancer
  selector:
    app: backend_deployment
  ports:
    - port: 80
      protocol: TCP
      targetPort: 3000
      nodePort: 31019
